# Baseline проекта на 2026-09-15

## Идентификаторы

- Репозиторий: `bav56172-svg/andrey-portfolio`
- Локальный клон: `/Users/andraybalashov/Projects/andrey-portfolio`
- Основная ветка: `master`
- Baseline commit: `d687f7f5a3ff467939d45121e0592123ef039abc`
- Baseline subject: `[agent] tune: B.A.C.E.Y теперь первый в списке проектов`
- Публичный URL: <https://bav56172-svg.github.io/andrey-portfolio/>

## Состав

- Next.js `14.2.35`, React `18.3.1`, TypeScript `5.5.x`.
- App Router: `app/page.tsx`, `app/layout.tsx`, `app/globals.css`.
- Одна статическая страница; данные навыков и проектов находятся внутри `page.tsx`.
- Один локальный публичный asset: `public/avatar.jpg`.
- Статический экспорт с `basePath: /andrey-portfolio`.
- GitHub Actions workflow для Pages присутствует; последний проверенный run для baseline commit завершился успешно.
- Сгенерированный каталог `out/` отслеживается Git.
- README, PDT и ADR до EP-001 отсутствовали.

## Текущая информационная архитектура

1. Hero: фото, доступность, имя, специализация, вводный текст, два CTA.
2. Терминальный блок `$ whoami`.
3. Навыки: восемь тегов.
4. Проекты: B.A.C.E.Y, SPES76, ЕЦУЭПО.
5. Контакт: Telegram.

## Проверки baseline

- `npm ci`: завершено, установлено 28 пакетов.
- `npm run build`: статический экспорт сформирован; `out/index.html` присутствует.
- `npm audit`: 2 уязвимые зависимости — 1 high и 1 critical; корневой пакет риска `next`, транзитивный `postcss`.
- Автоматический `npm audit fix --force` не запускался, потому что предложенное исправление ведёт к major-обновлению Next.js и требует отдельной миграции.

## Риски и расхождения

1. GitHub Pages API сообщает `build_type: legacy` и источник `gh-pages`, а `.github/workflows/deploy.yml` успешно собирает и развёртывает `master` через Pages Actions. Два действующих пути публикации создают неоднозначность; в отдельном пакете нужно выбрать один источник истины.
2. `out/` в Git создаёт шум при каждой сборке и риск расхождения с исходниками.
3. В package scripts нет отдельного lint или test.
4. Линия Next.js 14 содержит известные уязвимости по текущему `npm audit`.
5. Компонент страницы перегружен inline-стилями, что затрудняет поддержку и последовательную адаптивность.
6. Описания проектов пока не оформлены как доказательные кейсы.

## Ограничение baseline

Этот документ фиксирует состояние исходного commit. Он не утверждает, что публичная страница побайтно соответствует `master`: это проверяется отдельно после выбора и документирования production-процесса.
